apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: backend-read-config
  namespace: demo-app
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    resourceNames: ["app-config"]
    verbs: ["get"]
